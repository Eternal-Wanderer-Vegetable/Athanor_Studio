# B2 — DOCX 原生 OOXML 读取：计划存档 + 实施

按你的指示分两步：① 把计划存档到 design_docs；② 按计划实施。所有提交信息用英文。

## 第一步：计划存档（P0）

新建 `design_docs/B2 — DOCX 原生 OOXML 读取：RFC 与执行计划.md`（沿用 M6 文档体例）：
- §1 可执行验收标准（9 条：语料 verify、映射表逐行断言、annotations/theme schema 校验、tracked changes、Pandoc-free 测试、回退不回归、友好失败、确定性黄金比对）
- §2 复用盘点（ImportJob/ImportOutput 契约、preserved 机制、quick-xml 0.42 已在 Cargo.lock、IdGen、relocate_annotations；已知缺陷：docx converter 名误用 athanor-html）
- §3 核心决策 11 条：azodoc-docx::ooxml 子模块（不建新 crate）；quick-xml + 轻量元素树；OoxmlPackage 三层解析（Content-Types/rels）；损失按 feature 聚合；批注→semantics/annotations.json（text_quote→block→document 降级）；tracked changes 终稿视图+原件 preserved（双快照修订链留后续）；styles→theme.json（slug 规则 ^[a-z][a-z0-9-]*$，原名存 title 额外字段）；页眉页脚/OLE/文本框整件 preserved（OLE 用 preserved_quarantined）；OMML 保留+Degraded（数学场景用 pandoc 回退）；确定性+安全（归档序遍历、zip-slip 消毒、尺寸护栏、四段式错误）；CLI --reader auto|native|pandoc + converter 名修复（athanor-docx / athanor-docx-pandoc）
- §4 OOXML→Azodoc 映射表（部件级 + 正文元素级，逐行 class/action，落实 SDOC 草案 §21）
- §5 分阶段计划 P0–P4；§6 难点对策；§7 影响面；§8 v1 排除项
另在 Future Work §B2 加状态注记。提交：`docs(b2): add native DOCX OOXML reader RFC and execution plan`

## 第二步：实施（P1–P4，每阶段收口后英文提交）

- **P1**：`engine/crates/azodoc-docx/src/ooxml/`（xml.rs 元素树、package.rs 遍历器+消毒+护栏、error.rs 四段式）+ 正文映射（段落/标题/run b-i-u-strike/超链接/编号多级列表/表格 gridSpan+vMerge/图片资产/脚注）+ `import_native` 接线进 `azodoc_docx::import` + CLI `--reader` + ImportOutput 扩展 + 单元测试。前置：GitNexus impact 分析（import、cmd_import、ImportOutput 等被编辑符号）。
- **P2**：styles→theme.json 写入器 + 块 `style` 引用 + sectPr→defaults.page；页眉/页脚整件 preserved；core.xml → DocumentMeta（creator 走 doc_extra）。
- **P3**：comments→annotations（cmd_import 写 semantics/annotations.json + manifest 登记）；tracked changes 终稿视图 + document.xml preserved + 聚合事件。
- **P4**：`corpus/docx/{basic,lossy}.docx`（确定性生成脚本）+ 黄金测试（AZODOC_WRITE_GOLDEN）+ 友好失败套件（截断 zip/缺 Content-Types/加密/路径穿越）+ e2e（verify 通过、pandoc 回退不回归）+ README 与 loss spec feature token 登记更新；全仓 fmt/clippy/test 绿。
- 每阶段提交前跑 GitNexus detect-changes；提交信息英文（如 `feat(docx): native OOXML package walker and core body mapping`）。